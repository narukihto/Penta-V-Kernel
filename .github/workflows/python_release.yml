name: Penta-V Kernel PyPI Release

on:
  push:
    branches: [ main, Heartbeat ] # Updated to include your current active branch
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]
  # Manual Trigger: Enables the "Run workflow" button in GitHub UI
  workflow_dispatch: 

jobs:
  test:
    name: Rust Unit & Integration Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
      - name: Run Stability & Benchmark Tests
        run: cargo test --verbose

  build_wheels:
    name: Build Python Wheels (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: test
    strategy:
      matrix:
        # Build matrix for multi-platform distribution
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Build Wheels via Maturin
        uses: PyO3/maturin-action@v1
        with:
          command: build
          # Technical: Optimization for release builds
          args: --release --out dist
          sccache: 'true'
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}
          path: dist

  publish_pypi:
    name: Production Release (PyPI)
    runs-on: ubuntu-latest
    needs: build_wheels
    # Critical: Publish only on version tags or manual trigger for testing
    if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          path: dist
      - name: Publish to PyPI
        uses: PyO3/maturin-action@v1
        env:
          # Required: PYPI_API_TOKEN must be stored in Repository Secrets
          MATURIN_PYPI_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        with:
          command: upload
          args: --non-interactive --skip-existing dist/*
