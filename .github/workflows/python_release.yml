name: Penta-V Kernel Sovereign Release

on:
  push:
    # Monitoring core architecture, heartbeat logic, and implementation branches
    branches: [ main, Heartbeat, ikom ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]
  workflow_dispatch: 

jobs:
  test:
    name: Core, Stability & Performance Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Run Integrated Test Suite
        # Validate v0.4.0 core logic and HRL stabilization
        run: cargo test --verbose --features default

      - name: Verify Sovereign Integration
        # Ensure synergy between AI Logic Validation and security primitives
        run: cargo test --test integration_suite --features default

      - name: Execute Sovereign Benchmarks
        # Capture sub-nanosecond performance metrics (Resonance Pass)
        run: |
          cargo bench --bench resonance_bench -- --output-format bencher | tee benchmark_result.txt
        continue-on-error: true

      - name: Upload Performance Artifacts
        # Archive Criterion HTML reports for performance audit and documentation
        uses: actions/upload-artifact@v4
        with:
          name: resonance-performance-report
          path: target/criterion/

  build_wheels:
    name: Build Sovereign Wheels (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: test
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Build Global Compatibility Wheels
        uses: PyO3/maturin-action@v1
        with:
          command: build
          # Release mode with HRL & Sovereign-pack features enabled
          args: --release --out dist --features extension-module --features sovereign-pack
          sccache: 'true'
          manylinux: auto 

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}
          path: dist

  publish_pypi:
    name: Sovereign PyPI Deployment
    runs-on: ubuntu-latest
    needs: build_wheels
    if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
    environment: release
    steps:
      - name: Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          # Isolate wheels to prevent uploading benchmark directories (Fixes OS Error 21)
          pattern: wheels-*
          merge-multiple: true
          path: dist

      - name: Deploy to PyPI
        uses: PyO3/maturin-action@v1
        env:
          MATURIN_PYPI_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        with:
          command: upload
          # Explicitly target binaries to ensure zero-drift deployment
          args: --non-interactive --skip-existing dist/*.whl dist/*.tar.gz
